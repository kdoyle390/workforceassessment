<h1>Organizations</h1>

<table>
  <thead>
    <tr>
      <th>Organization Name</th>
      <th>Hourly Rate</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @organizations.each do |organization| %>
      <tr>
        <td><%= Organization.name %></td>
        <td><%= Organization.hourly_rate %></td>
        <td><%= link_to "Choose", book %></td>
        <td><%= link_to "Edit", edit_book_path(book) %></td>
        <%# <td><%= link_to "Destroy", organization, data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %></td> %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to "New organization", new_organization_path %>